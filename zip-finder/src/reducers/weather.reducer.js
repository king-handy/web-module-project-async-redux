const initialState = {
    city: '',
    state: '',
    zip: ''
}

export const weatherReducer = (state = initialState, action) => {
    switch(action.type) {
        default:
            return state
    }
}
