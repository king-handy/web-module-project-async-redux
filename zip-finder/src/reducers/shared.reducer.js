const initialState = {
    isLoading: false,
    title: 'Zip Finder'
}

export const sharedReducer = (state = initialState, action) => {
    switch(action.type) {
        default:
            return state
    }
}
